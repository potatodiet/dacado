apply plugin: 'java'

sourceCompatibility = 1.7
version = '0.1'

repositories {
  mavenCentral()
}

dependencies {
  testCompile group: 'junit', name: 'junit', version: '3.0'
}

task buildJar(type: Jar) {
  manifest {
    attributes 'Main-Class': 'com.pyrohail.dacado.Main'
  }
  from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } }
  with jar
}
